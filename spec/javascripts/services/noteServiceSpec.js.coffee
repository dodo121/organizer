describe 'NotesCtrl', ->
  describe 'getAll', ->

  describe 'destroy', ->
    it 'removes note', ->

  describe 'saveNote', ->
    describe 'when note is new', ->
      it 'creates new Note', ->

    describe 'when note is not new', ->
      it 'updates existing note', ->